
{{! The big featured header for the Homepage, with the tagline }}

<header class="site-head" {{#if @blog.cover}}style="background-image: url({{@blog.cover}})"{{/if}}>
    <div class="vertical">
        <div class="site-head-content inner">
            {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="Blog Logo" /></a>{{/if}}
            
            {{!--
            <h1 class="blog-title">{{@blog.title}}</h1>
            <h2 class="blog-description">{{@blog.description}}</h2> 
            --}}

            <div class="masthead">
                <div class="text">
                    This is the home of <strong>Salman Suhail</strong>, product manager and technologist. <br>
                    Using the power of the internet to make <strong>the world a better place.</strong>
                </div>
            </div>
        
            <br>
            
        </div>
    </div>
</header>
